apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  namespace: mr-do-openhab
  name: mqtt-pvc-data
spec:
  storageClassName: ""
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
  selector:
    matchLabels:
      usage: mqtt-pv-data
