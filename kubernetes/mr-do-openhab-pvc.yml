apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  namespace: mr-do-openhab
  name: mr-do-openhab-pvc-data
spec:
  storageClassName: mr-nfs
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 4Gi
  selector:
    matchLabels:
      usage: mr-pv-data
